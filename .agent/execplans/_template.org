#+TITLE: ExecPlan Title
#+AUTHOR: CyberXiuXian Artisan workshop
#+FILETAGS: :agent:execplan:
#+DATE: YYYY-MM-DD Day HH:MM:SS

* TODO ExecPlan Title [0/11] [0%] :execplan:
:PROPERTIES:
:SDD: <sdd-path-or-none>
:ORG_TASK: <org-task-path>
:STABLE_REFS: <repo-relative-stable-reference>
:PACKAGE: <package-or-subsystem>
:SLICE: <slice-slug>
:COMMAND_POLICY: RTK.md
:COOKIE_DATA: direct
:NEXT_ACTION: <next-action>
:RESUME_QUERY: <resume-query>
:ARCHIVE_TARGET: <archive-target-path>
:END:
SCHEDULED: <YYYY-MM-DD Day>

- [ ] Plan self check complete.
- [ ] Task-local research complete.
- [ ] Implementation complete.
- [ ] Validation complete.
- [ ] Quality audit complete.
- [ ] Recovery and archive state updated.

** Purpose / Big Picture

** Scope and Boundaries

*** Files or Dirs to Read

- [ ] List only task-local target files and directories.

*** Commands or Tools to Run

- [ ] List exact validation and inspection commands.

*** RTK Project Command Guide

- Rust focused checks: =direnv exec . rtk --ultra-compact cargo test -p <crate> <filter> --lib=.
- Rust compile/lint gates: =direnv exec . rtk --ultra-compact cargo check -p <crate>= and =direnv exec . rtk --ultra-compact cargo clippy --all-targets --all-features -- -D warnings=.
- TypeScript/package checks: =direnv exec . rtk --ultra-compact npm run check=, =direnv exec . rtk --ultra-compact npm run build=, or the package-specific =npm run <script>=.
- Python checks: =direnv exec . rtk --ultra-compact pytest -q= and =direnv exec . rtk --ultra-compact ruff check <path>= when the wrapper applies.
- Git and small inspections outside the project environment: =rtk git status --short=, =rtk git diff -- <paths>=, =rtk read <path>=, and =rtk ls <path>=.
- Search policy: prefer raw =rg= for fast repository search; use RTK wrappers only when they provide useful filtering for the command being run.
- Org recovery and lint are ASP-owned. Query tasks with =asp query playbook --documents org --kind task --term '<remembered text>'=, resolve IDs with =asp query playbook --documents org --kind property --field key=ID --field value=<org-section-id>=, and lint with =asp org lint <path>=.
- Julia, artifact-producing, parser-output, or exact-transcript commands: use raw =direnv exec .= unless =rtk rewrite <command>= shows a real filtered wrapper.

*** Expected Outputs

- [ ] Define concrete implementation, documentation, and evidence outputs.

*** Stop Conditions

- [ ] Define conditions that should stop implementation or require replanning.

** TODO Plan Self Check [0/9] [0%]
:PROPERTIES:
:COOKIE_DATA: direct
:END:

- [ ] Scope matches the request and risk level.
- [ ] Files or dirs to read are complete and minimal.
- [ ] Commands or tools to run follow =RTK.md=: use =direnv exec . rtk <filtered-command>= for project-scoped commands with a real RTK wrapper, prefer =direnv exec . rtk --ultra-compact <filtered-command>= for high-volume validation gates, and record exceptions.
- [ ] High-volume Rust, Python, and TypeScript checks use the project command guide above when summaries are sufficient.
- [ ] Expected outputs are concrete and testable.
- [ ] Stop conditions are clear.
- [ ] Dependencies and constraints are recorded.
- [ ] Validation plan is adequate for risk.
- [ ] Plan type is correct.

** Context and Orientation

** Plan of Work

** TODO Concrete Steps [0/5] [0%]
:PROPERTIES:
:COOKIE_DATA: direct
:END:

- [ ] Task-local research complete.
- [ ] Implementation patch complete.
- [ ] Tests or lint checks updated.
- [ ] Targeted validation passed.
- [ ] Progress, decisions, and recovery notes updated.

** TODO Validation and Acceptance [0/5] [0%]
:PROPERTIES:
:COOKIE_DATA: direct
:END:

- [ ] Functional acceptance criteria satisfied.
- [ ] Regression tests or equivalent focused checks passed.
- [ ] Shell-command evidence follows =RTK.md=; high-volume checks use =--ultra-compact= when summaries are enough, and =rtk proxy= / =rtk run= are used only as passthrough exceptions, not token-saving wrappers.
- [ ] Documentation or tracking surfaces updated.
- [ ] Hidden-path Markdown link scan considered when canonical docs changed.

** TODO Reflection and Quality Audit [0/3] [0%]
:PROPERTIES:
:COOKIE_DATA: direct
:END:

- [ ] Code audit recorded.
- [ ] Plan audit recorded.
- [ ] Verification audit recorded.

** TODO Final Validation Gate [0/3] [0%]
:PROPERTIES:
:COOKIE_DATA: direct
:END:

- [ ] Validation and Acceptance is complete.
- [ ] Reflection and Quality Audit is complete.
- [ ] Final go/no-go decision recorded.

** Idempotence and Recovery

** Interfaces and Dependencies

** Progress

Record dated checkpoints here. Keep the newest checkpoint precise enough that a
future agent turn can continue without scanning chat history first.

** Decision Log

** Surprises & Discoveries

** Artifacts and Notes

** Outcomes & Retrospective

Record final evidence, validation commands, and any achievement tag that should
remain queryable after archival.

** Change Log

** Recovery Commands

#+begin_src text
asp query playbook --documents org --kind task --term '<remembered text>'
asp query playbook --documents org --kind property --field key=ID --field value=<org-section-id>
asp org lint $PRJ_CACHE_HOME/agent/org
#+end_src
