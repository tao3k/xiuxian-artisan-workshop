# ORG Parser Snapshot Slot

This directory is reserved for upcoming ORG parser snapshot contracts.

When ORG parsing is introduced, add parser snapshots here and unignore
`org_parser_snapshot_contract_placeholder` in `tests/parser_snapshots/org.rs`.
