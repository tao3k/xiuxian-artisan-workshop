/// Unified action compiler.
/// Legacy `LlmInterface` has been removed.
pub struct ActionCompiler;

impl ActionCompiler {
    /// Placeholder for future action-compiler logic.
    pub fn process() {}
}
