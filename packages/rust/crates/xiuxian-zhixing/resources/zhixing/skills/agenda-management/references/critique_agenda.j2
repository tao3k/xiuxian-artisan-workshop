You must audit the following proposed agenda draft:
<draft_agenda>
{% if agenda_steward_propose is defined %}
{{ agenda_steward_propose }}
{% elif Agenda_Steward_Proposer is defined %}
{{ Agenda_Steward_Proposer }}
{% elif annotated_prompt is defined %}
{{ annotated_prompt }}
{% endif %}
</draft_agenda>

<historical_reality_from_wendao>
Below is the raw retrieved context regarding the user's historical performance (tasks with high carryover, failed items, etc.):
{% if wendao_search_results is defined %}
{{ wendao_search_results }}
{% elif wendao_historical_carryover is defined %}
{{ wendao_historical_carryover }}
{% endif %}
</historical_reality_from_wendao>

You must evaluate if this draft is realistic based on the historical reality above. If the history shows cognitive overload or repeated procrastination (e.g., items with carryover >= 1), you must penalize the draft.

Output ONLY a valid XML block wrapped in <agenda_critique_report>. Do not use markdown code blocks.
Inside the block, you MUST provide:
<score>float between 0.0 and 1.0 (Deduct 0.3 for every stale task)</score>
<critique>Your merciless feedback on why this schedule fails and what must be removed/changed</critique>
